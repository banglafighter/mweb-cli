name: "This is the Project Name"

startScript:
  - script 1
  - script 2

dependencies:
  - name: "app-base-dependencies"
    dir: "dependencies"
    status: "active"

    module:
      status: "inactive"
      script:
        - pip install -e .
      sourceDir:
        - name: "example-module"
          script:
            - pip install -e .

    clone:
      branch: "dev"
      status: "active"
      script:
        - pip install -e .
      repo:
        - url: "https://example.com/example.git"
          dir: "project-name"
          branch: "pre-release"
          script:
            - python --version


endScript:
  - script 1
  - script 2